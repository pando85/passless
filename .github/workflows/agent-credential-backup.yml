name: Credential backup implementation bootstrap

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap paused while payload is uploaded"
